import 'package:flutter/material.dart';

class WeatherByDay extends StatelessWidget {
  const WeatherByDay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
