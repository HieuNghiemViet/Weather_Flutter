import 'package:flutter/material.dart';

class Constant {
  static const kBorderRadius = BorderRadius.all(Radius.circular(20.0));
  static const kEdgeInsets = EdgeInsets.only(left: 10.0, right: 10.0, bottom: 20.0);
}
